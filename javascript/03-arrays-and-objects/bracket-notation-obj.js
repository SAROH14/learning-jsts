let newTestObj = {
    "an entree": "hamburguer",
    "my side": "veggies",
    "the drink": "water"
};

let entreeValue = newTestObj["an entree"];   //bracket required if name of the property has a space
let drinkValue = newTestObj["the drink"];