function isLess(x, y){
    
    /* 
    if (x < y){

        return true;
    } else {
        return false;
    } */

    // cumbersome code above. instead it can be simplified into:

    return x < y; // the < comparison dictates if true or false. since x is less then it is true. if x is greater then it is false.



}


console.log(isLess(10, 15));