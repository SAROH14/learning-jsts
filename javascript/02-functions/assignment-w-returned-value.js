let changed = 0;

function change(num){

    return (num + 5) / 3;
}

changed = changed(10); // is assigned the returned value from changed(10)

let processed = 0;

function processedArg(num){
    return (num + 3) / 5;

}

processed = processedArg(7); // is assigned the returned value from processedArg(7);



